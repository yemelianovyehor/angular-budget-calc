import { NotZeroValidatorDirective } from './not-zero-validator.directive';

describe('NotZeroValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NotZeroValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
